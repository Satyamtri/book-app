<div class="container">
    <div class="row g-4">
       <div class="col-md-3">
       <% @categories.each do |category| %>
         <%= category.title%>
        <% end%>
          </div>
    </div>
</div>